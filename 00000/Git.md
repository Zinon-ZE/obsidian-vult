[[Git]]
### To Research

$$Algorithms$$
- The Random Agent(RNG)
- The 

test auto pull


### Git Commands used 

`git init ` -  takes the local directory as starting point 
`git clone` - clones the repo from the github
`git checkout -b` - creates a new branch ?
`git checkout *branch name*` - switch branches]]
`git branch` - list branches and shows which branch ur in
`git remote add upstream` - 
`git remote -v` -
`git fetch upstream` -
`git status` - checking changes
`git add --all` - staging all changes happening in repo
`git add . vs git add --all` -
`git reset` - unstage all the changes
`git commit -m " blh blh "` - commit changes to the branch and -m is comment 

##### Git Pushing to github
`git push origin main` - send local repo to github

##### Git Author related

	`git config --global user.name "zinon" user.email "winkoko10825@gmail.com`
